{
    "name": "nuxt-rendering-demo",
    "version": "0.1.0",
    "private": true,
    "author": "Alex Schouls",
    "scripts": {
        "build": "nuxt build",
        "clean": "npx rimraf .nuxt .output dist && npx nuxi prepare",
        "dev": "nuxt dev",
        "generate": "nuxt generate",
        "postinstall": "nuxt prepare",
        "preview": "nuxt preview"
    },
    "dependencies": {
        "contentstack": "^3.17.1",
        "nuxt": "^3.4.2"
    },
    "devDependencies": {
        "@types/node": "^18",
        "prettier": "^2.8.8"
    }
}
